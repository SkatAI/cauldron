services:
  cauldron:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./config:/app/config
    command: uvicorn cauldron.main:app --reload --host 0.0.0.0 --port 8000
