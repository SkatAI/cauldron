services:
  cauldron:
    build: .
    ports:
      - "8088:8088"
    env_file:
      - .env.local
    volumes:
      - ./src:/app/src
      - ./docs/prompts:/app/docs/prompts
    command: uvicorn cauldron.main:app --reload --host 0.0.0.0 --port 8088
